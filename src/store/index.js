import { createStore } from 'vuex'
import { data } from './data'
import { lang } from './lang'

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    data,
    lang
  }
})
